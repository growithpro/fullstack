name: Deploy Backend to render

on:
    push:
        branches: [ main ]

jobs:
    deploy:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout Code
              uses: actions/checkout@v4

            - name: Render Ko Bata Dena
              run: |
                  curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK }}